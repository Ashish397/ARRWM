Harden merged release: de-anonymise, restore label map, fix CPU-hostile imports

Applied on top of the local-agent merge, which predated several of these and
reverted them:

- De-anonymisation (shipping blocker for an anonymous submission): removed
  /home/ashish, /projects/u5as and as1748.u5as paths from three eval scripts
  and two preprocessing scripts; all now derive from DATA_ROOT / WAN_MODELS /
  AF_ROOT. Repo-wide scan is clean.
- Restored the shared figure label map (figures/figure_labels.py), which the
  merge had reverted to per-file hardcoded labels; kept the merge's new
  nocritic run and added it to the canonical map as "No Critic".
- pre_encode_motion.py had no main() and no __name__ guard, so its whole
  ride-processing loop -- including loading CoTracker onto CUDA -- ran at
  import time and failed on any GPU-less machine. Wrapped in main(); device
  falls back to CPU. Operations and constants unchanged.
- Added module docstrings to 11 files including both entry points; pruned 17
  unused imports; stripped trailing whitespace.

Verification tooling and evidence (verification/, tools/): figure layer
re-rendered from the release against real data; PCA basis refit and compared
against the shipped artifact; A/B training harness that runs the pre-cleanup
repo and code_release under identical seed and node count.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
