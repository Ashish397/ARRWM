# Derived from the imports this release actually uses. Install PyTorch first,
# matched to your CUDA version (see pytorch.org); the pin below is the build the
# reported runs used.

# --- core: training, inference, data ---
torch==2.10.0
torchvision
numpy
einops
omegaconf
zarr
numcodecs
diffusers==0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
peft
wandb

# --- video / image IO ---
opencv-python>=4.9.0.80
av==13.1.0
imageio
imageio-ffmpeg
pillow

# --- Wan T5 text encoder tokenisation ---
ftfy
regex
sentencepiece

# --- captioning + ride metadata (preprocessing/ride_level_caption.py) ---
geopy                 # reverse-geocodes ride GPS into the caption
pytz                  # ride-local timestamps
PyYAML                # caption prompt/config files

# --- analysis and figures ---
pandas
scipy
matplotlib
tqdm

# --- reference-free evaluation metrics ---
lpips                 # perceptual distance, stationary reference metrics
scikit-image          # SSIM, stationary reference metrics
timm                  # DINO backbones for the drift instruments
pyiqa                 # MUSIQ / NIQE image-quality scores
open_clip_torch       # CLIP embeddings for the style-shift axes
datasets              # HuggingFace loader used by the caption pipeline

# --- optional ---
# flash-attn is imported behind a try/except in wan/modules/attention.py. The
# causal model used by every released config runs on PyTorch's flex_attention
# and never calls into it, so it is not required to reproduce the results. Only
# the bidirectional WanModel path (is_causal=False, unused here) asserts it.
# flash-attn
